import React from 'react';
import Button from 'react-bootstrap/Button';

export default function Predict() {
    return (
        <div>
            <Button variant="dark">Upload Slide Image</Button>
        </div>
    );
}
